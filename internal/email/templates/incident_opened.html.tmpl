<!doctype html>
<html>
  <body style="font-family: sans-serif; color: #1a1a1a;">
    <p>An incident was opened for <strong>{{.ServiceName}}</strong>{{if .TenantName}} in {{.TenantName}}{{end}}.</p>
    <p><strong>{{.IncidentTitle}}</strong>{{if .Severity}} (severity: {{.Severity}}){{end}}</p>
    <p><a href="{{.DashboardURL}}">View the incident</a></p>
  </body>
</html>
